# Force npm to use the correct platform binaries
target_platform=linux
target_arch=arm64
target_libc=glibc

# Disable optional dependencies that might cause issues
optional=false

# Use npm for better platform handling
package-manager=npm
